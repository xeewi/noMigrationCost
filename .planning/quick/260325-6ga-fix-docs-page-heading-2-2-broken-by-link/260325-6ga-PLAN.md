---
phase: quick
plan: 260325-6ga
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/DocsSidebar.tsx
autonomous: true
must_haves:
  truths:
    - "Sidebar heading 2.2 displays clean text without markdown link syntax"
    - "Clicking heading 2.2 in sidebar scrolls to the correct section in the document"
  artifacts:
    - path: "src/components/DocsSidebar.tsx"
      provides: "extractHeadings with markdown link stripping"
  key_links:
    - from: "DocsSidebar extractHeadings slug"
      to: "rehype-slug generated id"
      via: "both process stripped text identically"
      pattern: "doc-22-types-of-maintenance"
---

<objective>
Fix extractHeadings in DocsSidebar.tsx so markdown link syntax is stripped from heading text before display and slug generation.

Purpose: Heading 2.2 currently shows raw `[[12]](url)` in the sidebar and generates a slug that does not match rehype-slug's output, breaking anchor navigation.

Output: Clean sidebar text and matching slugs for all headings containing inline markdown links.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/DocsSidebar.tsx
@src/components/DocsPage.tsx

The affected heading in the markdown:
```
### 2.2 Types of Maintenance and Their Distribution [[12]](https://galorath.com/blog/software-maintenance-costs/) [[13]](https://maddevs.io/customer-university/software-maintenance-costs/)
```

rehype-slug processes rendered HTML text (after links resolve), so it sees:
"2.2 Types of Maintenance and Their Distribution [12] [13]"
and produces slug: `doc-22-types-of-maintenance-and-their-distribution-12-13`

extractHeadings sees raw markdown and produces a different slug because it includes URL text in the toSlug input.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip markdown links from extracted heading text</name>
  <files>src/components/DocsSidebar.tsx</files>
  <action>
Add a `stripMarkdownLinks` helper function above `extractHeadings`:

```typescript
/** Strip markdown link syntax [text](url) -> text, preserving link text */
function stripMarkdownLinks(s: string): string {
  return s.replace(/\[([^\]]*)\]\([^)]*\)/g, '$1').trim();
}
```

In `extractHeadings`, change line 32 from:
```typescript
const text = m[2].trim();
```
to:
```typescript
const text = stripMarkdownLinks(m[2]);
```

This handles `[[12]](url)` -> `[12]` (outer brackets are plain text, inner `[12]` is the link text). The result matches what rehype-slug sees, so toSlug produces the same slug.
  </action>
  <verify>
    <automated>cd /Users/brucecoaster/code/noMigrationCost && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Heading 2.2 sidebar text reads "2.2 Types of Maintenance and Their Distribution [12] [13]" (no URLs)
    - Generated slug matches rehype-slug output so clicking scrolls to correct section
    - Build passes with no TypeScript errors
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. Dev server: sidebar heading 2.2 shows clean text
3. Clicking heading 2.2 in sidebar scrolls to the correct document section
</verification>

<success_criteria>
- All sidebar headings display clean text without raw markdown link syntax
- Sidebar anchor links for headings with inline markdown links scroll to the correct section
- No regressions in other sidebar heading links
</success_criteria>

<output>
After completion, create `.planning/quick/260325-6ga-fix-docs-page-heading-2-2-broken-by-link/260325-6ga-SUMMARY.md`
</output>
