---
phase: 04-scaffold-engine-and-standalone-cost
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - src/main.tsx
  - src/App.tsx
  - src/index.css
  - src/vite-env.d.ts
  - src/lib/utils.ts
  - src/components/ui/
  - index.html
  - components.json
autonomous: true
requirements:
  - TEAM-01
  - TEAM-02
  - TEAM-04

must_haves:
  truths:
    - "Vite dev server starts and renders a React component in the browser"
    - "shadcn components can be imported and rendered without errors"
    - "Tailwind CSS 4 utility classes apply visible styles"
    - "Vitest runs and passes a trivial test"
    - "The @/ path alias resolves in both Vite and Vitest"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "react"
    - path: "vite.config.ts"
      provides: "Vite + Tailwind 4 + Vitest config"
      contains: "@tailwindcss/vite"
    - path: "src/lib/utils.ts"
      provides: "cn() utility and formatEuro() helper"
      exports: ["cn", "formatEuro"]
    - path: "src/App.tsx"
      provides: "Root React component"
      contains: "function App"
    - path: "components.json"
      provides: "shadcn configuration"
      contains: "aliases"
  key_links:
    - from: "src/main.tsx"
      to: "src/App.tsx"
      via: "React DOM createRoot render"
      pattern: "createRoot.*App"
    - from: "vite.config.ts"
      to: "@tailwindcss/vite"
      via: "Vite plugin import"
      pattern: "tailwindcss"
---

<objective>
Scaffold the Vite + React 19 + TypeScript + Tailwind CSS 4 + shadcn/ui project from scratch, install all Phase 4 dependencies, initialize shadcn, add all required UI components, and configure Vitest.

Purpose: Establish the project foundation that all subsequent plans depend on. Nothing can be built without a working scaffold.
Output: A running Vite dev server with shadcn components available, Vitest configured, and the cn()/formatEuro() utilities ready.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-scaffold-engine-and-standalone-cost/04-CONTEXT.md
@.planning/phases/04-scaffold-engine-and-standalone-cost/04-RESEARCH.md
@.planning/phases/04-scaffold-engine-and-standalone-cost/04-UI-SPEC.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React project and install all dependencies</name>
  <files>package.json, vite.config.ts, tsconfig.json, tsconfig.app.json, tsconfig.node.json, src/main.tsx, src/App.tsx, src/index.css, src/vite-env.d.ts, index.html</files>
  <read_first>
    - .planning/phases/04-scaffold-engine-and-standalone-cost/04-RESEARCH.md (Installation section and Vitest Config section)
    - CLAUDE.md (tech stack constraints)
  </read_first>
  <action>
1. Run `npm create vite@latest . -- --template react-ts` to scaffold the project in the current directory. If prompted about existing files, proceed (only .planning/ and docs/ exist).

2. Install core dependencies:
   ```
   npm install recharts lucide-react clsx tailwind-merge
   ```

3. Install Tailwind CSS 4 with Vite plugin (NOT PostCSS):
   ```
   npm install tailwindcss @tailwindcss/vite
   ```

4. Install dev dependencies for testing:
   ```
   npm install -D vitest @testing-library/react @testing-library/user-event jsdom
   ```

5. Configure `vite.config.ts` with Tailwind 4 Vite plugin, path alias, and Vitest:
   ```typescript
   import { defineConfig } from 'vite'
   import react from '@vitejs/plugin-react'
   import tailwindcss from '@tailwindcss/vite'

   export default defineConfig({
     plugins: [react(), tailwindcss()],
     resolve: {
       alias: { "@": "/src" },
     },
     test: {
       environment: 'jsdom',
       globals: true,
     },
   })
   ```

6. Replace `src/index.css` content with Tailwind 4 single import (NOT v3 directives):
   ```css
   @import "tailwindcss";
   ```
   Do NOT use `@tailwind base; @tailwind components; @tailwind utilities;` — those are Tailwind v3 syntax.
   Do NOT create `tailwind.config.js` or `postcss.config.js` — Tailwind 4 uses the Vite plugin.

7. Update `src/App.tsx` to a minimal placeholder that proves Tailwind works:
   ```typescript
   function App() {
     return (
       <div className="min-h-screen bg-background text-foreground p-8">
         <h1 className="text-2xl font-semibold">Feature Cost Calculator</h1>
         <p className="text-muted-foreground mt-2">Scaffold ready.</p>
       </div>
     )
   }
   export default App
   ```

8. Add `"test": "vitest run"` script to package.json scripts if not present.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "react", "recharts", "tailwindcss", "@tailwindcss/vite", "vitest"
    - vite.config.ts contains `tailwindcss()` in plugins array
    - vite.config.ts contains `alias: { "@": "/src" }`
    - vite.config.ts contains `test:` block with `environment: 'jsdom'`
    - src/index.css contains `@import "tailwindcss"` and does NOT contain `@tailwind`
    - No `tailwind.config.js` or `postcss.config.js` exists
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Vite project scaffolded with React 19, Tailwind CSS 4 (Vite plugin), Vitest configured, all dependencies installed, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Initialize shadcn and add all Phase 4 UI components</name>
  <files>components.json, src/lib/utils.ts, src/components/ui/</files>
  <read_first>
    - vite.config.ts (verify current state after Task 1)
    - src/index.css (verify Tailwind 4 import is present)
    - .planning/phases/04-scaffold-engine-and-standalone-cost/04-RESEARCH.md (shadcn init section, component list)
    - .planning/phases/04-scaffold-engine-and-standalone-cost/04-UI-SPEC.md (Component Inventory table)
  </read_first>
  <action>
1. Run `npx shadcn@latest init` to initialize shadcn. Use the following settings:
   - Style: Default (New York if prompted)
   - Base color: Zinc
   - CSS variables: Yes
   IMPORTANT: Use `shadcn` NOT `shadcn-ui` (the old package is deprecated per D-20 research).
   If the CLI adds a `postcss.config.js`, delete it — Tailwind 4 uses the Vite plugin, not PostCSS.

2. After init, verify that `src/lib/utils.ts` was created by shadcn with the `cn()` function. If not, create it:
   ```typescript
   import { type ClassValue, clsx } from "clsx"
   import { twMerge } from "tailwind-merge"

   export function cn(...inputs: ClassValue[]) {
     return twMerge(clsx(inputs))
   }
   ```

3. Add the `formatEuro()` utility to `src/lib/utils.ts` (below `cn()`):
   ```typescript
   export function formatEuro(amount: number): string {
     if (!isFinite(amount) || isNaN(amount)) return '\u2014';
     return new Intl.NumberFormat('fr-FR', {
       style: 'currency',
       currency: 'EUR',
       maximumFractionDigits: 0,
     }).format(amount);
   }
   ```
   The em-dash (\u2014) is used for invalid/zero-team empty states per UI-SPEC.

4. Add all shadcn components needed for Phase 4 (per UI-SPEC Component Inventory):
   ```
   npx shadcn@latest add card table input select tabs button toggle badge separator
   ```
   This installs components into `src/components/ui/`.

5. Verify the path alias works with shadcn by checking that generated components use `@/lib/utils` imports.

6. If shadcn init modified `src/index.css`, verify it still contains `@import "tailwindcss"` and that shadcn's CSS variables are present (theme tokens like `--background`, `--foreground`, etc.).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - components.json exists and contains `"aliases"` key
    - src/lib/utils.ts contains `export function cn(`
    - src/lib/utils.ts contains `export function formatEuro(`
    - src/lib/utils.ts contains `Intl.NumberFormat('fr-FR'`
    - src/components/ui/card.tsx exists
    - src/components/ui/table.tsx exists
    - src/components/ui/input.tsx exists
    - src/components/ui/select.tsx exists
    - src/components/ui/tabs.tsx exists
    - src/components/ui/button.tsx exists
    - src/components/ui/toggle.tsx exists
    - src/components/ui/badge.tsx exists
    - src/components/ui/separator.tsx exists
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>shadcn initialized with zinc theme, all 9 Phase 4 components installed, cn() and formatEuro() utilities available, build still passes</done>
</task>

<task type="auto">
  <name>Task 3: Create engine type definitions and verify Vitest runs</name>
  <files>src/engine/types.ts, src/engine/__tests__/smoke.test.ts</files>
  <read_first>
    - .planning/phases/04-scaffold-engine-and-standalone-cost/04-RESEARCH.md (Pattern 1: Pure Engine Module, type definitions)
    - .planning/phases/04-scaffold-engine-and-standalone-cost/04-CONTEXT.md (D-05 through D-09 for seniority grid, D-19 through D-21 for engine)
    - docs/feature-cost-shared-vs-duplicated.md (sections 1.3 and 7.1 for salary defaults and formula structure)
    - vite.config.ts (verify test config exists)
  </read_first>
  <action>
1. Create `src/engine/types.ts` with all type definitions the engine and UI will share. These are the contracts for Plan 02 (engine) and Plan 03 (UI):

   ```typescript
   export type SeniorityLevel = 'Junior' | 'Mid' | 'Senior' | 'Lead';

   export interface SeniorityRow {
     label: SeniorityLevel;
     headcount: number;
     hourlyRate: number; // euro/h, user-overridable
   }

   export type SizingMode = 'story-points' | 'direct-hours';
   export type DirectHoursUnit = 'hours' | 'days';

   export interface SizingInputs {
     mode: SizingMode;
     storyPoints: number;
     velocity: number;       // SP per sprint
     sprintWeeks: number;    // 1-4
     directValue: number;
     directUnit: DirectHoursUnit;
   }

   export interface EngineInputs {
     teamAvgHourlyRate: number;
     devHours: number;
     horizonYears: number;
     maintenanceRate: number;         // default 0.18 shared, 0.22 duplicated
     generalizationFactor: number;    // default 1.3
     portingFactor: number;           // default 0.65
     divergenceRate: number;          // default 0.20
     bugDuplicationFactor: number;    // default 2.0
     nbConsumingCodebases: number;    // default 2
   }

   export interface BreakdownRow {
     category: string;
     hours: number;
     cost: number;
     percentage: number; // 0-100
   }

   export interface StandaloneOutputs {
     teamAvgHourlyRate: number;
     initialDevCost: number;
     initialDevHours: number;
     annualMaintenanceCost: number;
     totalMaintenanceCost: number;
     totalStandaloneCost: number;
     breakdown: BreakdownRow[];
   }

   export interface SharedCostOutputs {
     initialDevCost: number;
     libSetupCost: number;
     annualMaintenanceCost: number;
     annualCoordinationCost: number;
     annualOnboardingCost: number;
     totalCost: number;
     yearlyBreakdown: YearCost[];
   }

   export interface DuplicatedCostOutputs {
     duplicatedDevCost: number;
     yearlyBreakdown: YearCost[];
     totalCost: number;
   }

   export interface YearCost {
     year: number;
     cumulativeCost: number;
   }

   export interface BreakEvenResult {
     exists: boolean;
     months: number | null;
   }

   /** Default salary values from docs/feature-cost-shared-vs-duplicated.md section 1.3 */
   export const SENIORITY_DEFAULTS: Record<SeniorityLevel, number> = {
     Junior: 32,
     Mid: 40,
     Senior: 51,
     Lead: 67,
   } as const;

   /** French legal working hours per week */
   export const HOURS_PER_WEEK = 35;

   /** French legal working hours per day */
   export const HOURS_PER_DAY = 7;

   /** Default engine parameters from research doc */
   export const ENGINE_DEFAULTS = {
     generalizationFactor: 1.3,
     portingFactor: 0.65,
     divergenceRate: 0.20,
     maintenanceRateShared: 0.18,
     maintenanceRateDuplicated: 0.22,
     bugDuplicationFactor: 2.0,
     nbConsumingCodebases: 2,
     defaultHorizonYears: 5,
   } as const;
   ```

2. Create `src/engine/__tests__/smoke.test.ts` as a trivial test to verify Vitest works with the @ alias:
   ```typescript
   import { describe, it, expect } from 'vitest';
   import { SENIORITY_DEFAULTS, HOURS_PER_WEEK } from '@/engine/types';

   describe('engine types smoke test', () => {
     it('exports seniority defaults matching French loaded costs', () => {
       expect(SENIORITY_DEFAULTS.Junior).toBe(32);
       expect(SENIORITY_DEFAULTS.Mid).toBe(40);
       expect(SENIORITY_DEFAULTS.Senior).toBe(51);
       expect(SENIORITY_DEFAULTS.Lead).toBe(67);
     });

     it('uses French 35h work week', () => {
       expect(HOURS_PER_WEEK).toBe(35);
     });
   });
   ```

3. Run `npx vitest run` to confirm tests pass and the @ alias resolves.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/engine/types.ts contains `export type SeniorityLevel`
    - src/engine/types.ts contains `export interface EngineInputs`
    - src/engine/types.ts contains `export interface StandaloneOutputs`
    - src/engine/types.ts contains `export interface SharedCostOutputs`
    - src/engine/types.ts contains `export interface BreakEvenResult`
    - src/engine/types.ts contains `SENIORITY_DEFAULTS` with values Junior: 32, Mid: 40, Senior: 51, Lead: 67
    - src/engine/types.ts contains `HOURS_PER_WEEK = 35`
    - src/engine/types.ts contains `ENGINE_DEFAULTS` with generalizationFactor: 1.3, portingFactor: 0.65
    - src/engine/__tests__/smoke.test.ts exists
    - `npx vitest run` exits 0 with all tests passing
  </acceptance_criteria>
  <done>Engine type contracts defined for all Phase 4 and Phase 5 formula functions, Vitest confirmed working with @ alias, smoke tests pass</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0 (Vite builds without errors)
- `npx vitest run` exits 0 (smoke tests pass)
- `src/components/ui/` contains card, table, input, select, tabs, button, toggle, badge, separator
- `src/engine/types.ts` exports all shared types and constants
- `src/lib/utils.ts` exports cn() and formatEuro()
- No `tailwind.config.js` or `postcss.config.js` exists (Tailwind 4 uses Vite plugin)
</verification>

<success_criteria>
- Vite dev server starts and renders React content
- All 9 shadcn components installed and importable
- Vitest runs with @ path alias working
- Engine type contracts established for downstream plans
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-scaffold-engine-and-standalone-cost/04-01-SUMMARY.md`
</output>
