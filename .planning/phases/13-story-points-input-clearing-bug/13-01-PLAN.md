---
phase: 13-story-points-input-clearing-bug
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/FeatureSizing.tsx
autonomous: true
requirements:
  - VEL-FIX

must_haves:
  truths:
    - "User can fully clear the velocity input field and see a placeholder"
    - "User can type a new velocity value (e.g. 42) after clearing without fighting the old value"
    - "Engine receives velocity=0 while field is empty and does not error or divide by zero"
    - "All other numeric inputs (story points, direct hours) continue to work unchanged"
  artifacts:
    - path: "src/components/FeatureSizing.tsx"
      provides: "Fixed velocity input with empty-when-zero pattern"
      contains: "velocity === 0 ? '' : velocity"
  key_links:
    - from: "src/components/FeatureSizing.tsx"
      to: "src/engine/formulas.ts"
      via: "App.tsx state -> calcDevHours(sizing) where sizing.velocity may be 0"
      pattern: "velocity === 0.*return 0"
---

<objective>
Fix the Team Velocity (SP/sprint) input so the user can fully clear the field and type a new value. Currently the onChange handler clamps NaN to 1, preventing the user from erasing the existing value.

Purpose: Remove UX friction — users cannot retype velocity without awkward selection-and-overwrite.
Output: Updated FeatureSizing.tsx with the empty-when-zero pattern matching story points and direct hours inputs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-story-points-input-clearing-bug/13-CONTEXT.md

@src/components/FeatureSizing.tsx
@src/engine/formulas.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix velocity input to allow clearing</name>
  <files>src/components/FeatureSizing.tsx</files>
  <read_first>
    - src/components/FeatureSizing.tsx (current velocity input at lines 107-116, reference pattern at lines 90-100)
    - src/engine/formulas.ts (velocity=0 guard at line 119)
  </read_first>
  <action>
In src/components/FeatureSizing.tsx, modify the velocity Input component (lines 107-116) to match the established empty-when-zero pattern used by story points (line 94) and direct hours (line 151). Three changes:

1. Change the `value` prop from:
   `value={velocity}`
   to:
   `value={velocity === 0 ? '' : velocity}`

2. Add a `placeholder` prop:
   `placeholder="1"`

3. Change the onChange handler NaN/min guard from:
   `onVelocityChange(isNaN(v) || v < 1 ? 1 : v)`
   to:
   `onVelocityChange(isNaN(v) || v < 0 ? 0 : v)`

   This allows the intermediate state of 0 (displayed as empty via step 1). The engine already guards velocity=0 in calcDevHours (returns 0 dev hours), so no division-by-zero occurs.

4. Change `min={1}` to `min={0}` on the Input element to match the new behavior.

Do NOT change anything else in the file — story points, direct hours, sprint duration, and all other inputs remain untouched (per D-03, they already work correctly).
  </action>
  <verify>
    <automated>cd /Users/brucecoaster/code/noMigrationCost && grep -n "velocity === 0 ? '' : velocity" src/components/FeatureSizing.tsx && grep -n "placeholder=\"1\"" src/components/FeatureSizing.tsx && grep -n "isNaN(v) || v < 0 ? 0 : v" src/components/FeatureSizing.tsx && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - src/components/FeatureSizing.tsx contains `value={velocity === 0 ? '' : velocity}`
    - src/components/FeatureSizing.tsx contains `placeholder="1"` on velocity input
    - src/components/FeatureSizing.tsx contains `onVelocityChange(isNaN(v) || v < 0 ? 0 : v)`
    - src/components/FeatureSizing.tsx contains `min={0}` on velocity input (not min={1})
    - Story points input unchanged: still contains `value={storyPoints === 0 ? '' : storyPoints}`
    - Direct hours input unchanged: still contains `value={directValue === 0 ? '' : directValue}`
    - `npm run build` succeeds with no TypeScript errors
  </acceptance_criteria>
  <done>Velocity input can be fully cleared (shows placeholder "1"), user can type new value directly, engine handles velocity=0 gracefully, build passes</done>
</task>

</tasks>

<verification>
1. `npm run build` passes — no TypeScript or compilation errors
2. `grep "velocity === 0 ? '' : velocity" src/components/FeatureSizing.tsx` matches exactly 1 line
3. Manual: open dev server, go to Story Points tab, clear velocity field — field empties and shows "1" placeholder, type "42" — field shows 42, dev hours update correctly
</verification>

<success_criteria>
- Velocity input follows the same empty-when-zero pattern as story points and direct hours
- User can clear the field completely and type a new value without fighting the old value
- Build succeeds, no regressions in other inputs
</success_criteria>

<output>
After completion, create `.planning/phases/13-story-points-input-clearing-bug/13-01-SUMMARY.md`
</output>
