---
phase: 6
slug: url-sharing
status: draft
shadcn_initialized: true
preset: base-nova / neutral / css-variables
created: 2026-03-24
---

# Phase 6 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | shadcn | components.json |
| Preset | base-nova, baseColor: neutral, cssVariables: true | components.json |
| Component library | Radix UI (via shadcn base-nova style) | components.json |
| Icon library | lucide-react | components.json iconLibrary: "lucide" |
| Font | Geist Variable (imported via @fontsource-variable/geist) | src/index.css |

**New component to install before implementation:**
```bash
npx shadcn@latest add alert-dialog
```
Source: RESEARCH.md Standard Stack — shadcn/ui AlertDialog not yet installed; required for D-05 reset confirmation.

---

## Spacing Scale

Declared values (multiples of 4). Existing layout uses Tailwind spacing utilities; all values map to this 8-point scale.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps (e.g., gap between icon and label on Copy Link button) |
| sm | 8px | Compact element spacing, inline button padding |
| md | 16px | Default element spacing, card content padding |
| lg | 24px | Section padding, gap between header and two-column layout |
| xl | 32px | Layout gaps between major sections |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions:
- Copy Link button and Reset All button minimum touch target: 36px height (shadcn Button default h-9 = 36px — adequate for desktop presentation tool; no mobile exception needed per REQUIREMENTS.md out-of-scope)
- Header padding matches existing page container: `px-6 py-8` inherited from `max-w-[1280px] mx-auto px-6 py-8` wrapper in App.tsx

Source: Existing App.tsx layout patterns; REQUIREMENTS.md (mobile layout out of scope).

---

## Typography

Extracted from codebase scan of `src/components/*.tsx`. This phase introduces no new typographic roles; the AppHeader component uses only the existing scale.

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 14px (text-sm) | 400 (font-normal) | 1.5 | Helper text, descriptions, muted labels |
| Label | 14px (text-sm) | 500 (font-medium) | 1.5 | Form labels, table cells, button text |
| Heading | 20px (text-xl) | 600 (font-semibold) | 1.2 | Card titles (consistent with all existing cards) |
| Display | 30px (text-3xl) | 600 (font-semibold) | 1.0 | Large cost figures in CostOutput (existing pattern) |

Phase 6 new elements:
- App title in header: 20px / font-semibold (text-xl font-semibold) — matches card title weight so it feels integrated, not louder
- Button labels (Copy Link, Reset All): 14px / font-medium — Shadcn Button default

Source: grep of src/components/*.tsx for existing text-* and font-* classes.

---

## Color

All values reference existing CSS custom properties from `src/index.css` (shadcn neutral base, OKLCH scale, light mode shown).

| Role | CSS Variable | Approximate Value | Usage |
|------|-------------|-------------------|-------|
| Dominant (60%) | --background | oklch(1 0 0) — white | Page background, AppHeader background |
| Secondary (30%) | --card / --muted | oklch(1 0 0) / oklch(0.97 0 0) — near-white / light gray | Cards, muted surfaces |
| Accent (10%) | --primary | oklch(0.205 0 0) — near-black | Reserved for specific elements only (see below) |
| Destructive | --destructive | oklch(0.577 0.245 27.325) — red | AlertDialog confirm button (Reset destructive action only) |

Accent (--primary) reserved for:
- The Copy Link button in its default state (variant="default" — filled primary background, signals the primary shareable action for the phase)
- The checkmark icon during "Copied!" feedback state
- The AlertDialog confirm/action button (`AlertDialogAction` — shadcn default renders as primary)

The Reset All button uses variant="outline" — secondary/border treatment, not accent — so it does not compete with Copy Link.

Source: src/index.css color definitions; CONTEXT.md D-01 (Copy Link is the primary phase CTA); CONTEXT.md D-05 (reset is a secondary/destructive action).

---

## Component Inventory

New components introduced in this phase:

| Component | File | shadcn Dependencies | Purpose |
|-----------|------|---------------------|---------|
| AppHeader | src/components/AppHeader.tsx | Button (already installed), AlertDialog (to install) | Houses app title, Copy Link button, Reset All button with confirmation |
| url-state module | src/lib/url-state.ts | None (browser native btoa/atob) | encode/decode/serialize/deserialize state to URL hash |

Existing components modified:
| Component | Modification |
|-----------|-------------|
| src/App.tsx | Add `useEffect` for hash write (debounced, 300ms); add `useEffect` for hash read on mount; convert Tabs to controlled state; render `<AppHeader>` above two-column layout; pass reset handler to AppHeader |

No new shadcn/ui primitive components are needed beyond AlertDialog.

Source: RESEARCH.md Architecture Patterns; CONTEXT.md D-02.

---

## AppHeader Visual Specification

The header is intentionally lightweight — it must not compete with the calculator content below it.

**Layout:** Full-width bar above the two-column layout. Uses the same `max-w-[1280px] mx-auto px-6` container as the page body. No background color change — stays `bg-background` (same as page). A bottom border (`border-b border-border`) provides visual separation without adding visual weight.

**Height:** `py-3` (12px top/bottom padding) — compact; taller than a toolbar but shorter than a navigation bar.

**Structure (flex, space-between):**
```
[App title — left]    [Copy Link button | Reset All button — right]
```

**App title:** "Feature Cost Calculator" — text-base (16px) font-semibold text-foreground. Not text-xl (that would match card titles and feel like a section heading, not a page title at this compact height).

**Copy Link button:**
- Default state: `variant="default"` — primary-filled button with Link icon (lucide `Link` icon, h-4 w-4) and label "Copy Link"
- Copied state: icon swaps to lucide `Check` (h-4 w-4), label changes to "Copied!" — reverts after 2000ms
- Size: `size="sm"` — matches compact header height

**Reset All button:**
- `variant="outline"` — secondary treatment, visually quieter than Copy Link
- Label: "Reset All"
- Size: `size="sm"`
- Wrapped in AlertDialog trigger

**Separator between buttons:** `gap-2` (8px) between the two buttons.

Source: CONTEXT.md D-01, D-02, D-03, D-04; RESEARCH.md Pattern 4; existing button variant patterns in codebase.

---

## AlertDialog (Reset Confirmation) Specification

Triggered by the Reset All button in AppHeader. Uses shadcn/ui AlertDialog (to be installed).

| Element | Copy | Notes |
|---------|------|-------|
| Dialog title | "Reset all inputs?" | Concise question format — matches shadcn AlertDialog convention |
| Dialog description | "This will clear your entire scenario. This action cannot be undone." | Two sentences: what happens + consequence |
| Cancel button | "Cancel" | AlertDialogCancel — outline/ghost; dismisses, no action taken |
| Confirm button | "Reset" | AlertDialogAction — renders as destructive; executes reset |

The confirm button should use destructive styling. Apply `className="bg-destructive text-destructive-foreground hover:bg-destructive/90"` to `AlertDialogAction` to match the `--destructive` color token.

Source: CONTEXT.md D-05; RESEARCH.md Pattern 5; src/index.css --destructive token.

---

## Copywriting Contract

| Element | Copy | Source |
|---------|------|--------|
| Primary CTA (Copy Link default) | "Copy Link" | CONTEXT.md D-03 — inline button feedback |
| Primary CTA (Copy Link copied state) | "Copied!" | CONTEXT.md D-03 — brief inline state change |
| App title | "Feature Cost Calculator" | CLAUDE.md project name |
| Reset button label | "Reset All" | CONTEXT.md D-04 |
| AlertDialog title | "Reset all inputs?" | Derived from D-05 + AlertDialog convention |
| AlertDialog description | "This will clear your entire scenario. This action cannot be undone." | Derived from D-05 — must convey irreversibility |
| AlertDialog cancel | "Cancel" | Standard dismissal label |
| AlertDialog confirm | "Reset" | Confirms the destructive action |
| Empty state heading | N/A | Phase 6 adds no new empty states; existing calculator empty states from Phases 4-5 unchanged |
| Error state (malformed URL) | Silent fallback to defaults — no user-visible error message | RESEARCH.md Pattern 3: malformed hash → graceful fallback; no error UI needed |
| Clipboard error (HTTPS failure) | Silent degradation — "Copied!" feedback is suppressed; no error toast | RESEARCH.md Pitfall 5; no toast component installed |

Copywriting notes:
- "Copied!" uses exclamation for positive micro-feedback. One word only — no icon-only state.
- "Reset All" uses title case to match existing button convention in the codebase (e.g., "Advanced Parameters" labels).
- AlertDialog description avoids technical language ("state", "hash", "URL parameters") — plain language only.

Source: CONTEXT.md Decisions; RESEARCH.md Pitfalls 4 and 5.

---

## Interaction States

| Element | State | Visual Treatment |
|---------|-------|-----------------|
| Copy Link button | Default | variant="default" (primary filled), Link icon |
| Copy Link button | Copied (2s) | Same variant, icon swaps to Check, label "Copied!" |
| Copy Link button | Hover | shadcn Button default hover — background slightly lighter |
| Copy Link button | Disabled | Not applicable — always available (URL always shareable) |
| Reset All button | Default | variant="outline" |
| Reset All button | Hover | shadcn Button outline hover — background: muted |
| AlertDialog | Closed | Not rendered |
| AlertDialog | Open | Overlay + modal with title/description/Cancel/Reset |
| AlertDialog Cancel | Click | Dialog closes, no state change |
| AlertDialog Reset | Click | Dialog closes, all state setters called with defaults, hash cleared |

URL hash behavior (no visible UI state):
- Hash updates 300ms after any input change (debounced) — user sees browser URL bar update silently
- On mount with valid hash: all inputs restore instantly (no loading state, no animation)
- On mount with no hash: defaults used (current behavior, unchanged)
- On mount with malformed hash: defaults used silently

Source: CONTEXT.md D-01, D-03, D-04, D-05; RESEARCH.md Patterns 2, 3, 4, 5.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | alert-dialog | not required — official registry |

No third-party registries declared. No vetting gate required.

Source: RESEARCH.md Standard Stack; CONTEXT.md has no third-party registry references.

---

## Layout Integration

Phase 6 inserts the AppHeader above the existing two-column layout. The change to App.tsx JSX structure:

**Before (Phase 5):**
```
<div class="min-h-screen bg-background text-foreground">
  <div class="max-w-[1280px] mx-auto px-6 py-8">
    <div class="flex gap-8 ...">  {/* two-column layout */}
```

**After (Phase 6):**
```
<div class="min-h-screen bg-background text-foreground">
  <AppHeader onReset={handleReset} />          {/* new — full-width header with border-b */}
  <div class="max-w-[1280px] mx-auto px-6 py-8">
    <div class="flex gap-8 ...">  {/* two-column layout unchanged */}
```

The `py-8` on the content container remains unchanged. The header is visually separated by border-b, not by extra margin above the content.

Source: CONTEXT.md code context; RESEARCH.md recommended project structure.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
