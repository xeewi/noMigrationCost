---
phase: 07-author-footer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/AppFooter.tsx
  - src/App.tsx
autonomous: false
requirements:
  - FOOT-01
  - FOOT-02
  - FOOT-03
  - FOOT-04

must_haves:
  truths:
    - "User can see 'Made by Guillaume Gautier (xeewi)' in a fixed footer visible without scrolling"
    - "User can click GitHub icon in footer and land on https://github.com/xeewi in a new tab"
    - "User can click Malt icon in footer and land on https://www.malt.fr/profile/xeewi in a new tab"
    - "User can click LinkedIn icon in footer and land on https://www.linkedin.com/in/xeewi in a new tab"
    - "Footer does not overlap or hide any calculator content when scrolled to the bottom"
  artifacts:
    - path: "src/components/AppFooter.tsx"
      provides: "Fixed footer component with author name and icon links"
      exports: ["AppFooter"]
    - path: "src/App.tsx"
      provides: "Root layout with AppFooter rendered and bottom padding added"
      contains: "AppFooter"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/AppFooter.tsx"
      via: "import { AppFooter } from '@/components/AppFooter'"
      pattern: "import.*AppFooter.*from.*@/components/AppFooter"
    - from: "src/components/AppFooter.tsx"
      to: "https://github.com/xeewi"
      via: "anchor href"
      pattern: 'href="https://github.com/xeewi"'
    - from: "src/components/AppFooter.tsx"
      to: "https://www.malt.fr/profile/xeewi"
      via: "anchor href"
      pattern: 'href="https://www.malt.fr/profile/xeewi"'
    - from: "src/components/AppFooter.tsx"
      to: "https://www.linkedin.com/in/xeewi"
      via: "anchor href"
      pattern: 'href="https://www.linkedin.com/in/xeewi"'
---

<objective>
Create a fixed footer banner displaying the author name (Guillaume Gautier / xeewi) with clickable icon links to GitHub, Malt, and LinkedIn profiles, and integrate it into the app layout.

Purpose: Establish author branding — every user sees attribution and can reach the author's professional profiles.
Output: AppFooter.tsx component + App.tsx integration with bottom padding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-author-footer/07-CONTEXT.md
@.planning/phases/07-author-footer/07-RESEARCH.md

<interfaces>
<!-- Key patterns from AppHeader.tsx that AppFooter must mirror -->

From src/components/AppHeader.tsx:
```tsx
// Container pattern — footer MUST use the same max-width/padding
<header className="border-b border-border">
  <div className="max-w-[1280px] mx-auto px-6 py-3 flex items-center justify-between">
```

// Icon sizing convention — h-4 w-4
<Check className="h-4 w-4" />
<Link className="h-4 w-4" />

// Named export pattern
export function AppHeader({ onReset }: AppHeaderProps) {
```

From src/App.tsx (lines 193-259):
```tsx
// Root layout structure — footer goes inside min-h-screen, after content div
<div className="min-h-screen bg-background text-foreground">
  <AppHeader onReset={handleReset} />
  <div className="max-w-[1280px] mx-auto px-6 py-8">
    {/* ... calculator content ... */}
  </div>
  {/* AppFooter goes here */}
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AppFooter component with inline SVG icons</name>
  <files>src/components/AppFooter.tsx</files>
  <read_first>
    - src/components/AppHeader.tsx (mirror container/border/padding pattern)
    - .planning/phases/07-author-footer/07-RESEARCH.md (verified SVG paths and complete component code)
  </read_first>
  <action>
Create `src/components/AppFooter.tsx` with the following exact implementation:

1. **Three inline SVG icon components** defined in the same file (NOT imported from lucide-react — brand icons removed in v1.0.1 per research):
   - `GitHubIcon({ className })` — viewBox="0 0 24 24", fill="currentColor", aria-hidden="true", path from simple-icons v16 CDN (see RESEARCH.md Code Examples for exact path data)
   - `MaltIcon({ className })` — viewBox="0 0 24 24", fill="currentColor", aria-hidden="true", path from simple-icons v16 CDN (see RESEARCH.md Code Examples for exact path data)
   - `LinkedInIcon({ className })` — viewBox="0 0 16 16" (Bootstrap Icons coordinate system), fill="currentColor", aria-hidden="true", path from Bootstrap Icons MIT (see RESEARCH.md Code Examples for exact path data)

2. **Named export `AppFooter` component** returning a `<footer>` element with:
   - Fixed positioning: `className="fixed bottom-0 left-0 right-0 z-50 border-t border-border bg-background"` (per D-01, D-02; z-50 prevents Recharts tooltips overlapping per research pitfall 4; bg-background prevents transparency per pitfall 3)
   - Inner container: `<div className="max-w-[1280px] mx-auto px-6 py-3 flex items-center justify-between">` (mirrors AppHeader exactly per D-02 and CONTEXT.md discretion)
   - Left side: `<span className="text-sm text-muted-foreground">Made by Guillaume Gautier (xeewi)</span>` (per D-04, D-05)
   - Right side: `<div className="flex items-center gap-1">` containing three icon links (per D-06)

3. **Each icon link** is a plain `<a>` tag (per research recommendation Option B — avoids tabIndex issues with Button wrapping):
   - GitHub: `href="https://github.com/xeewi"`, `aria-label="GitHub profile"`
   - Malt: `href="https://www.malt.fr/profile/xeewi"`, `aria-label="Malt profile"`
   - LinkedIn: `href="https://www.linkedin.com/in/xeewi"`, `aria-label="LinkedIn profile"`
   - All three: `target="_blank" rel="noopener noreferrer"` (per D-08)
   - All three: `className="inline-flex items-center justify-center size-8 rounded-lg text-muted-foreground hover:text-foreground hover:bg-muted transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"` (per D-09 hover effect)
   - Icon inside each: `<XxxIcon className="h-4 w-4" />` (matches lucide-react sizing convention from AppHeader)

Do NOT import Github or Linkedin from lucide-react — those exports do not exist in v1.0.1.
  </action>
  <verify>
    <automated>cd /Users/brucecoaster/code/noMigrationCost && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/AppFooter.tsx exists
    - File contains `export function AppFooter()`
    - File contains `function GitHubIcon(` and `function MaltIcon(` and `function LinkedInIcon(`
    - File contains `href="https://github.com/xeewi"`
    - File contains `href="https://www.malt.fr/profile/xeewi"`
    - File contains `href="https://www.linkedin.com/in/xeewi"`
    - File contains `target="_blank"` and `rel="noopener noreferrer"` (three occurrences)
    - File contains `aria-label="GitHub profile"` and `aria-label="Malt profile"` and `aria-label="LinkedIn profile"`
    - File contains `fixed bottom-0` and `z-50` and `border-t` and `bg-background`
    - File contains `Made by Guillaume Gautier (xeewi)`
    - File does NOT contain `import` from `lucide-react`
    - TypeScript compiles with no errors (`npx tsc --noEmit` exits 0)
  </acceptance_criteria>
  <done>AppFooter.tsx created with three inline SVG brand icons, author text, and three external links matching all locked decisions D-01 through D-09</done>
</task>

<task type="auto">
  <name>Task 2: Integrate AppFooter into App.tsx with bottom padding</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (current root layout — understand exact structure before editing)
    - src/components/AppFooter.tsx (just created — confirm export name)
  </read_first>
  <action>
Make three changes to `src/App.tsx`:

1. **Add import** — alongside the existing `import { AppHeader } from '@/components/AppHeader'`, add:
   ```
   import { AppFooter } from '@/components/AppFooter';
   ```

2. **Add bottom padding to content wrapper** (per D-03) — find the content div (currently `className="max-w-[1280px] mx-auto px-6 py-8"` around line 196) and change `py-8` to `py-8 pb-16`. The `pb-16` (64px) provides clearance above the fixed footer (~44px height per research calculation).

3. **Render AppFooter** — inside the `min-h-screen` wrapper div, after the content div closes (around line 258), add `<AppFooter />` before the closing `</div>` of the min-h-screen wrapper. The footer renders inside the wrapper but its `position: fixed` makes it stick to the viewport bottom.
  </action>
  <verify>
    <automated>cd /Users/brucecoaster/code/noMigrationCost && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/App.tsx contains `import { AppFooter } from '@/components/AppFooter'`
    - src/App.tsx contains `<AppFooter />`
    - src/App.tsx contains `pb-16` on the content wrapper div
    - TypeScript compiles with no errors
    - Vite build succeeds
  </acceptance_criteria>
  <done>AppFooter rendered in root layout, content has bottom padding preventing overlap, app builds successfully</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify footer appearance and links</name>
  <files>src/components/AppFooter.tsx, src/App.tsx</files>
  <action>
Human visual verification of the completed footer. Start dev server with `npm run dev` and verify in browser.

Check all of the following:
1. "Made by Guillaume Gautier (xeewi)" visible at viewport bottom without scrolling
2. Three icon buttons appear to the right of the text
3. Hover effect: icons shift from muted gray to full foreground color with subtle background
4. GitHub icon opens https://github.com/xeewi in new tab
5. Malt icon opens https://www.malt.fr/profile/xeewi in new tab
6. LinkedIn icon opens https://www.linkedin.com/in/xeewi in new tab
7. Scrolling to bottom: footer does not overlap calculator content
8. Footer styling mirrors header (same width, padding, border treatment)
  </action>
  <verify>
    <automated>cd /Users/brucecoaster/code/noMigrationCost && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>User confirms footer appearance, link destinations, hover states, and content clearance are all correct</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `npx tsc --noEmit` exits 0
- Build succeeds: `npm run build` exits 0
- Footer component exists: `src/components/AppFooter.tsx` exports `AppFooter`
- App.tsx imports and renders `<AppFooter />`
- App.tsx content wrapper has `pb-16` for footer clearance
- No lucide-react brand icon imports (would cause build failure)
</verification>

<success_criteria>
- Fixed footer banner visible on every page without scrolling (FOOT-01)
- GitHub icon link navigates to author's GitHub profile in new tab (FOOT-02)
- Malt icon link navigates to author's Malt profile in new tab (FOOT-03)
- LinkedIn icon link navigates to author's LinkedIn profile in new tab (FOOT-04)
- Footer does not obscure calculator content
- App builds and runs without errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-author-footer/07-01-SUMMARY.md`
</output>
